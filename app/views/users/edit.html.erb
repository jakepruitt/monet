<% title 'Profile' %>
<div class="edit row">
  <h2 class="column medium-6 medium-centered">Profile</h2>
  <%= form_for :user, url: user_path(current_user), :html => {:class => 'column medium-6 medium-centered'}, method: :patch do |f| %>
    <%= f.label :avatar, 'Avatar' %>
    <%= image_tag resolve_user_avatar(@user) %>
    <%= f.file_field :avatar %>
    <%= f.label :fullname, 'Full name' %>
    <%= f.text_field :fullname %>

    <div class="submit-field">
      <%= f.submit 'Update profile', :class => 'button' %>
    </div>

    <div class="other-links">
      <p><%= link_to '< Go back home', root_url %></p>
    </div>
  <% end %>
</div>
